{[template "backend/common/header_nonav.tmpl" .]}

<div class="layui-fluid">
    <div class="layui-col-md12" style="padding: 5px;">
        <div class="layui-card">
            <div class="layui-card-body">

                <div style="display:flex;align-items:center;gap:8px;margin-bottom: 1rem;">
                    <a href="/{[.admin_path]}/admin/recipients/instances" style="display:flex;align-items:center;">
                        <i class="layui-icon layui-icon-return"></i>  
                    </a>

                    <span style="margin:0;font-size:18px;">
                        {[if .Data]}
                            {[if eq .Data.MediaType "telegram" ]}telegram机器人{[else]}{[if eq .Data.MediaType "email" ]}邮件{[else]}{[if eq .Data.MediaType "webhook" ]}Webhook{[end]}{[end]}{[end]}
                        {[end]}
                    </span>
                </div>


                <div class="layui-tab layui-tab-brief">
                    <ul class="layui-tab-title" id="status">
                        <li data-status="1"><a href="/{[.admin_path]}/admin/recipients/instances/details?id={[.id]}">概览</a></li>
                        <li data-status="2"><a href="/{[.admin_path]}/admin/recipients/instances/update?id={[.id]}">修改</a></li>
                        <li data-status="3" class="layui-this">测试</li>
                    </ul>
                </div>

                <div class="layui-form toolbar" style="margin-top: 10px;margin-bottom: 10px;">
                <form class="layui-form layui-form-pane" onclick="return false;">
                    <table class="layui-table">
                        <colgroup><col width="150"><col></colgroup>
                        <tbody>
                            <tr>
                                <td>媒介名称</td>
                                <td>
                                    {[if .Data]}
                                        {[if eq .Data.MediaType "telegram" ]}Telegram机器人{[end]}
                                        {[if eq .Data.MediaType "email" ]}邮件{[end]}
                                        {[if eq .Data.MediaType "webhook" ]}WebHook{[end]}
                                    {[end]}
                                </td>
                            </tr>

                            <tr>
                                <td>通知标题</td>
                                <td>
                                    <input type="text" name="title" autocomplete="off" class="layui-input" placeholder="这是通知标题" value="这是通知标题">
                                </td>
                            </tr>

                            <tr>
                                <td>这是通知内容</td>
                                <td>
                                    <input type="text" name="content" autocomplete="off" class="layui-input" placeholder="这是通知内容" value="这是通知内容">
                                </td>
                            </tr>

                            <tr>
                                <td>接收人标识</td>
                                <td>
                                    <input type="text" name="send_id" autocomplete="off" class="layui-input" placeholder="" value="{[if .Data.GetTelegramParams]}{[.Data.GetTelegramParams.SendID]}{[end]}">
                                    <p class="layui-font-12">群或用户的Chat ID，通常是一个数字，可以通过和 @get_id_bot 建立对话并发送任意消息获得。</p>
                                </td>
                            </tr>

                            <tr>
                                <td colspan="2">
                                    <input type="hidden" name="id" value="{[if .Data]}{[.Data.ID]}{[end]}">
                                    <input type="hidden" name="media_type" value="{[if .Data]}{[.Data.MediaType]}{[end]}">
                                    <button class="layui-btn" lay-filter="submit_recipients_test" lay-submit>提交测试</button>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">

layui.config({base: '/static/layuiadmin/'}).use(['table','jquery'], function(){
///
var admin = layui.admin
,$ = layui.$
,form = layui.form
,table = layui.table;


form.on("submit(submit_recipients_test)", function(data){
    var index = layer.load();
    var field = data.field;

    $.post("/{[$.admin_path]}/admin/recipients/instances/test", field, function(res) {
        showMsg(res.msg, function(){
            layer.close(index);
            if(res.code > -1){
                // parent.location.reload();
            }
        },{icon: res.code > -1 ? 1 : 2,shift:res.code>-1 ? 0 : 6});
    },'json');
    return false;
});

///
});
</script>

{[template "backend/common/footer.tmpl" .]}
